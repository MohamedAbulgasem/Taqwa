<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug builds only. The recitation downloads are verified against a local HTTP server on the
  emulator's host (10.0.2.2) before the real releases exist; Android blocks cleartext by default
  and should. Nothing here reaches a release build: this file lives in src/debug and the release
  manifest names no network security config at all.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
